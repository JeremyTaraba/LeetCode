#include <vector>

class Solution {
public:
    bool uniqueOccurrences(vector<int>& arr) {
        vector<int> occ;
        //first sort the array then count number of occurences
        //then sort occurences and check if any values next to each other
        // are the same
        for(int i = 0; i < arr.size(); i++){
            
        }
    }
};